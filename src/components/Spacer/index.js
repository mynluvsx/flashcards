const Spacer = () => {
  return (
    <div
      style={{
        width: "100%",
        height: 1,
        backgroundColor: "rgba(0,0,0,0.5)",
        borderRadius: 16,
        filter: "blur(1px)",
        marginTop: "1rem",
        marginBottom: "1rem",
      }}
    />
  );
};

export default Spacer;
